local Page = require("build.App")

return function ()
    local items = {"a","b", "c", "d"}
    return
        <>
            {map(items,function(item)
                return <span>{item}</span>
            end)}
            <Page />
        </>
end
