local App = require("build.App")

function main()
    local items = {"coucoucouccoucouccou", "coucoucouccoucouccou", "coucocucou"}
    return
        <>
            {map(items,function(item)
                return <span>{item}</span>
            end)}
        </>
end

return main