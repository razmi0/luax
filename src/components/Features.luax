local Features = {}

function Features.Main(props)
  return (
    <div>
      {props.children}
    </div>
)
end
function Features.Grid(props)
  return (
    <div>
      {props.children}
    </div>
)
end

function Features.Card(props)
  return (
    <div>
      {props.children}
    </div>
)
end

function Features.Title(props)
  return (
    <h3>
      {props.children}
    </h3>
)
end

function Features.Description(props)
  return (
    <p>
      {props.children}
    </p>
)
end

return Features